# Ledger — TODOS

Generated by /plan-ceo-review on 2026-04-28. One-way door items flagged.

---

## P1 — Do Next

### Distribution Sprint
**What:** Firm-cohort invite links (pre-populate firm code from URL param), SEO meta
tags + og:image on all pages, sitemap.xml, fix 5 instances of `ledgercpa.vercel.app`
→ real domain in index.html (lines 843, 858, 2188, 10322, 10361).

**Why:** The design doc (April 17) flagged firm-cohort invites as "must be built for
launch" — the acquisition thesis depends on it. Zero users means distribution is the
binding constraint. Retention mechanics without acquisition is optimization on an
empty funnel.

**Depends on:** Retention mechanics sprint (current plan) — ship that first, then
immediately follow with distribution.

**Effort:** human ~1 day / CC ~2 hrs  
**Priority:** P1 — ship immediately after retention mechanics

---

## P2 — Next Quarter

### Firebase Cloud Messaging (FCM) Push Notifications
**What:** Real push notifications that reach users when the browser is closed. Requires
service worker registration + FCM setup in Firebase console.

**Why:** The current in-app nudge (shows banner on app open) validates that users
want reminders. FCM is the upgrade path once you have users who've opted into nudges
and want them off-device.

**When to build:** When 10+ users have been shown the in-app nudge and at least 5
have expressed wanting a real notification.

**Effort:** human ~1 day / CC ~2 hrs  
**Priority:** P2

---

### Weekly Email Digest for Study Groups
**What:** When 3+ users share a firm code, send a weekly email summary: "Your team
logged 42 hours this week. Top performer: Tyler (18 hrs)." Requires Firebase Trigger
Email extension or SendGrid.

**Why:** The design doc's core thesis is firm-cohort social accountability. A weekly
digest is the mechanism that makes study group accountability persistent — not just
when users open the app. High-value when cohorts exist.

**When to build:** When first firm cohort has 3+ active users.

**Effort:** human ~1 day / CC ~2 hrs  
**Priority:** P2

---

### Firebase Analytics Instrumentation
**What:** Add `logEvent()` calls for: `exam_result_logged`, `section_passed`,
`section_failed`, `notification_nudge_shown`, `pwa_prompt_shown/dismissed/installed`,
`pass_celebration_shared` (and which platform: linkedin/native/copy).

**Why:** You currently have no usage data. Once users arrive, you need to know
which features are used vs which are noise. The social share platform split is
especially valuable — it tells you if LinkedIn is actually the right channel.

**When to build:** Before first 10 users arrive.

**Effort:** CC ~30 min  
**Priority:** P2 (should move to P1 before launch)

---

## P3 — Future

### index.html Modularization
**What:** Split index.html (~10,477 lines and growing) into separate JS modules or
a simple build step (Vite or esbuild). At minimum, extract feature-specific logic
into separate files imported via `<script type="module">`.

**Why:** The file is 55% larger than it was 11 days ago. A second engineer (or AI
agent) will struggle to navigate it beyond ~15,000 lines. The right time is when
you know which features are permanent vs candidates for deletion.

**Trigger:** When index.html exceeds 12,000 lines OR after first 100 real users,
whichever comes first. **Note (added 2026-04-28):** The retention mechanics sprint
(7 features) is expected to push the file past 12,000 lines during implementation.
Trigger is imminent — plan the modularization before or immediately after that sprint ships.

**Effort:** human ~2 days / CC ~3 hrs  
**Priority:** P3 — not blocking, but don't defer past the trigger

---

### AI Coach Proxy (Firebase Cloud Function)
**What:** Move Anthropic API calls from direct browser → `api.anthropic.com` to a
Firebase Cloud Function that holds the API key server-side. Currently users must
supply their own `sk-ant-api03-...` key, which is a significant onboarding friction.

**Why:** Design doc identified this as the correct v2 architecture. Every user who
wants AI features currently needs their own Anthropic API key, which is a barrier.

**When to build:** When AI features show meaningful usage (10+ users using Ask Ledger).

**Effort:** human ~1 day / CC ~1 hr  
**Priority:** P3

---

### Question Bank Content Accuracy Review
**What:** Have a CPA licensee or Becker-trained candidate review the question bank
content for accuracy against the AICPA blueprint before removing the "Beta — verify
with your review course" disclaimer.

**Why:** Design doc flagged this as blocking for a real launch. Incorrect CPA content
is a trust and liability issue.

**When to build:** Before any marketing push or broader distribution.

**Effort:** human ~1-2 days (content review)  
**Priority:** P3 (but P1 before launch)

---

## Known Technical Debt (from CEO review)

- `sectionPassed[]` and `examResults[]` dual-write: resolved in retention sprint.
  `logExamResult()` uses a single `update()` call with `FieldValue.arrayUnion` for
  examResults and full array write for sectionPassed.
- Email verification URLs hardcoded to `ledgercpa.vercel.app` (lines 843, 858, 2188)
  — update when domain is locked in.
- canvas-confetti CDN tag: add SRI hash before production shipping.
- CSP check: verify Vercel headers don't block `cdn.jsdelivr.net` before adding
  the CDN script.
- Score input validation: frontend min="0" max="99" and JS validation in
  `logExamResult()` ship in retention sprint. Firestore security rule (reject writes
  with score outside 0–99 or missing required fields on examResults entries) is
  deferred — add to firebase security rules before any marketing push.
